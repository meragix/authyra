v0.1.0 Checklist — what
Must-have (blocking release)
 InMemoryAuthStorage — listed in the roadmap, used in all docs examples, but not yet implemented. Needed for tests to run without a real storage backend.
 Unit tests — packages/authyra/test/authyra_test.dart is void main() {}. Roadmap target: 80% coverage.
 Models: AuthUser, AuthSession, AuthState, AuthConfig, SessionRegistry
 SessionManager — save, load, update, clean expired, mutex correctness
 AccountManager — switchTo, signOut, signOutAll, cleanExpired
 AuthyraClient — signIn, signOut, refreshSession, registerProvider, initialize
 CredentialsProvider / CredentialsProvider.withTokens
 InMemoryAuthStorage (once implemented)
 melos run analyze — run dart analyze across all packages, fix any issues
 melos run publish:check — dry-run dart pub publish to verify pub.dev compliance
 pubspec.yaml topics — verify they match pub.dev guidelines (looks good already)
Nice-to-have (can ship as v0.1.1)
 introduction.md — remove the dead link to /flutter/setup
 CHANGELOG.md — move [Unreleased] items to [0.1.0] with release date
 melos run test:coverage — generate LCOV report and confirm ≥ 80%